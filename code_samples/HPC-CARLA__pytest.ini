[pytest]
testpaths = tests
# Default run = fast tier-1 (engine + config); heavy in-container tests opt-in.
addopts = -m "not heavy"
markers =
    heavy: requires torch/numpy and model weights; run in the container with `pytest -m heavy`
